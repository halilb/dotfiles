# ~/.tmuxinator/unifyre.yml

name: thanx
root: ~/thanx/app

windows:
  - server:
      layout: main-vertical
      panes:
        - git status
        - cd mobile && react-native start
  - editor: nvim
